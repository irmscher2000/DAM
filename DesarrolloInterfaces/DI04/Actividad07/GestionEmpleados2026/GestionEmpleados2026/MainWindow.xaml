<Window x:Class="GestionEmpleados2026.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GestionEmpleados2026"
        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="1024"
        WindowStartupLocation="CenterScreen">
    
    <StackPanel Orientation="Vertical">

        <TextBlock Text="Departamento RRHH"
                   FontSize="28"
                   FontWeight="Bold"
                   Margin="100"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"/>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="1"
                    Height="60"
                    Margin="20,0"
                    FontWeight="Medium"
                    FontSize="16"
                    Content="Lista de Empleados"
                    Click="btnListaEmpleados_Click"/>

            <Button Grid.Column="2"
                    Height="60"
                    Margin="20,0"
                    FontWeight="Medium"
                    FontSize="16"
                    Content="Agregar Empleados"
                    Click="btnAgregarEmpleado_Click"/>

            <Button Grid.Column="3"
                    Height="60"
                    Margin="20,0"
                    FontWeight="Medium"
                    FontSize="16"
                    Content="Buscar Empleado"
                    Click="btnBuscarEmpleado_Click"/>

        </Grid>
        
    </StackPanel>
        
    
</Window>
